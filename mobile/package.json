{
  "name": "mobile",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build:android": "cd ../frontend && powershell -Command \"if (Test-Path middleware.ts) { Rename-Item middleware.ts middleware.ts.bak -Force }\" && npx next build && powershell -Command \"if (Test-Path middleware.ts.bak) { Rename-Item middleware.ts.bak middleware.ts -Force }\" && cd ../mobile && npx cap sync android",
    "sync": "npx cap sync android",
    "open:android": "npx cap open android",
    "run:android": "npx cap run android",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@capacitor/android": "^8.1.0",
    "@capacitor/core": "^8.1.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.1.0"
  }
}